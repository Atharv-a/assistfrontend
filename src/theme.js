import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#fac278',
      dark: '#2F4858'
    },
    secondary: {
      main: '#3A8477',
      dark:'#AEB46A'
    },
  },
});

export default theme
